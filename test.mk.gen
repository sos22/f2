#! /bin/bash

. shutil

declare -a tests
tests=(testdriver.C
     testmap.C
     testpair.C
     testserialise.C
     teststring.C)
declare -a objs
for x in ${tests[@]}
do
    cc $x
    objs[${#objs[*]}]=${x/.C/.o}
done
ld test ${objs[@]} lib.a

target test
