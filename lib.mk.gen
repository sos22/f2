#! /bin/bash

. shutil

function bits() {
    echo {listenfd,logging,buffer,error,fd,mutex,obj,test,thread,unixsocket,waitbox,wireproto}$1
}

for x in $(bits .C)
do
    cc $x
done

ar lib.a $(bits .o)
