#! /bin/bash

. shutil

function bits() {
    echo {listenfd,logging,buffer,cond,error,fd,mutex,obj,proto,shutdown,test,thread,unixsocket,util,waitbox,wireproto}$1
}

for x in $(bits .C)
do
    cc $x
done

ar lib.a $(bits .o)
